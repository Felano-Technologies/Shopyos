# =====================================================
# SHOPYOS FRONTEND - ENV EXAMPLE
# Copy to .env and replace values
# =====================================================

# Dedicated socket service URL (used by services/socket.ts)
EXPO_PUBLIC_SOCKET_URL=http://localhost:5001

# Backend API base URL (used by services/api.tsx)
EXPO_PUBLIC_API_URL=http://localhost:5000
